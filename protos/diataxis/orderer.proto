syntax = "proto3";

package diataxis;

option go_package = "github.com/katastroma/diataxis";

// OrdererService sorts Kubernetes manifests into a safe apply order.
service OrdererService {
  rpc Order(stream OrderRequest) returns (OrderResponse);
}

// OrdererType identifies which ordering backend to use.
enum OrdererType {
  ORDERER_TYPE_UNSPECIFIED = 0;
  ORDERER_TYPE_CLI_UTILS = 1;
  ORDERER_TYPE_HELM = 2;
}

// Streamed unordered Kubernetes manifest.
message OrderRequest {
  bytes manifest = 1;
}

// Streamed ordered Kubernetes manifest.
message OrderResponse {}
